@using CinemaTicketBooking.Application
@using CinemaTicketBooking.Application.Features
@using CinemaTicketBooking.Domain
@model PagedResult<MovieDto>

@{
    ViewData["Title"] = "Quản lý phim";
}



<div class="glass-panel rounded-2xl border border-white/5 overflow-hidden shadow-2xl">
    <div class="px-8 py-6 flex flex-col md:flex-row md:items-center justify-between gap-4 border-b border-white/5 bg-white/[0.01]">
        <h4 class="font-headline font-bold text-xl text-on-surface flex items-center gap-3">
            <span class="w-2 h-2 rounded-full bg-secondary shadow-[0_0_8px_#00f4fe]"></span>
            Danh sách phim
        </h4>
        <div class="flex flex-col md:flex-row items-center gap-4 w-full md:w-auto">
            @if ((await AuthorizationService.AuthorizeAsync(User, Permissions.MoviesManage)).Succeeded)
            {
                <button onclick="openAddMovieModal()" class="w-full md:w-auto h-10 py-2.5 px-6 rounded-xl bg-gradient-to-r from-primary to-primary-container text-on-primary text-xs font-bold shadow-[0_4px_15px_rgba(97,180,254,0.3)] hover:shadow-[0_4px_25px_rgba(97,180,254,0.5)] active:scale-95 transition-all flex items-center justify-center gap-2 whitespace-nowrap">
                    <span class="material-symbols-outlined text-lg">add</span>
                    <span>Thêm Phim</span>
                </button>
            }
            <form method="get" class="w-full md:w-auto">
                <div class="flex items-center bg-surface-container-high rounded-xl border border-white/5 focus-within:border-primary/50 focus-within:ring-1 focus-within:ring-primary transition-all h-10">
                    <input type="text" name="searchTerm" id="searchInput" placeholder="Tìm kiếm phim..." class="flex-1 md:w-60 bg-transparent border-none text-xs text-slate-300 py-2.5 px-4 focus:ring-0" value="@Context.Request.Query["searchTerm"]" />
                    <div class="w-px h-4 bg-white/10"></div>
                    <button type="submit" id="searchBtn" class="px-3 h-full text-slate-400 hover:text-on-surface transition-colors flex items-center justify-center">
                        <span class="material-symbols-outlined text-sm" id="searchIcon">search</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
    
    <div class="overflow-x-auto">
        <table class="w-full text-left border-collapse min-w-[1000px]">
            <thead class="bg-white/[0.02]">
                <tr>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5">Phim</th>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5">Thể loại</th>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5">Thời lượng</th>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5">Hãng phim</th>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5">Trạng thái</th>
                    <th class="px-8 py-4 text-[10px] font-bold uppercase tracking-[0.2em] text-on-surface-variant border-b border-white/5 text-right">Thao tác</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-white/5">
                @if (Model.Items == null || !Model.Items.Any())
                {
                    <tr>
                        <td colspan="6" class="px-8 py-10 text-center text-sm text-on-surface-variant">Không tìm thấy phim nào.</td>
                    </tr>
                }
                else
                {
                    @foreach (var movie in Model.Items)
                    {
                        <tr class="hover:bg-white/[0.03] transition-colors group">
                            <td class="px-8 py-6">
                                <div class="flex items-center gap-4">
                                    <div class="w-12 h-16 rounded-lg overflow-hidden flex-shrink-0 bg-surface-container flex items-center justify-center">
                                        @if (!string.IsNullOrEmpty(movie.ThumbnailUrl))
                                        {
                                            <img src="@movie.ThumbnailUrl" class="w-full h-full object-cover" alt="@movie.Name poster" />
                                        }
                                        else
                                        {
                                            <span class="material-symbols-outlined text-2xl text-slate-500">movie</span>
                                        }
                                    </div>
                                    <div>
                                        <p class="font-headline font-bold text-on-surface group-hover:text-primary transition-colors">@movie.Name</p>
                                        <p class="text-[10px] text-on-surface-variant/60 font-medium">@movie.Director</p>
                                    </div>
                                </div>
                            </td>
                            <td class="px-8 py-6">
                                <span class="px-3 py-1 rounded-full bg-white/5 text-slate-300 text-[10px] font-bold border border-white/10">@movie.Genre</span>
                            </td>
                            <td class="px-8 py-6 text-sm font-bold text-on-surface">@movie.Duration phút</td>
                            <td class="px-8 py-6 text-sm text-on-surface-variant">@movie.Studio</td>
                            <td class="px-8 py-6">
                                @if (movie.Status == MovieStatus.NowShowing)
                                {
                                    <span class="px-3 py-1 rounded-full bg-secondary/10 text-secondary border-secondary/20 text-[10px] font-bold border uppercase">Đang chiếu</span>
                                }
                                else if (movie.Status == MovieStatus.Upcoming)
                                {
                                    <span class="px-3 py-1 rounded-full bg-primary/10 text-primary border-primary/20 text-[10px] font-bold border uppercase">Sắp chiếu</span>
                                }
                                else
                                {
                                    <span class="px-3 py-1 rounded-full bg-red-500/10 text-red-400 border-red-500/20 text-[10px] font-bold border uppercase">Ngừng chiếu</span>
                                }
                            </td>
                            <td class="px-8 py-6 text-right">
                                <div class="flex justify-end">
                                    @if ((await AuthorizationService.AuthorizeAsync(User, Permissions.MoviesManage)).Succeeded)
                                    {
                                        <button 
                                            data-id="@movie.Id"
                                            data-name="@movie.Name"
                                            data-desc="@movie.Description"
                                            data-thumb="@movie.ThumbnailUrl"
                                            data-studio="@movie.Studio"
                                            data-director="@movie.Director"
                                            data-trailer="@movie.OfficialTrailerUrl"
                                            data-duration="@movie.Duration"
                                            data-genre="@((int)movie.Genre)"
                                            onclick="openUpdateMovieModal(this)" 
                                            class="w-10 h-10 aspect-square flex items-center justify-center rounded-xl bg-white/5 text-slate-400 hover:text-primary hover:bg-primary/10 transition-all">
                                            <span class="material-symbols-outlined text-xl">edit</span>
                                        </button>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
    
    <!-- Pagination -->
    <div class="px-8 py-4 bg-white/[0.02] flex items-center justify-between">
        <p class="text-[10px] text-on-surface-variant font-medium">Hiển thị <span class="text-on-surface">@Model.Items?.Count() trên @Model.TotalItems</span> mục</p>
        <div class="flex items-center gap-2">
            @if (Model.PageNumber > 1)
            {
                <a href="?pageNumber=@(Model.PageNumber - 1)&searchTerm=@Context.Request.Query["searchTerm"]" class="w-10 h-10 aspect-square rounded-xl bg-white/5 text-slate-400 hover:bg-white/10 hover:text-on-surface transition-colors flex items-center justify-center">
                    <span class="material-symbols-outlined text-sm">chevron_left</span>
                </a>
            }
            @if (Model.PageNumber < Math.Ceiling((double)Model.TotalItems / Model.PageSize))
            {
                <a href="?pageNumber=@(Model.PageNumber + 1)&searchTerm=@Context.Request.Query["searchTerm"]" class="w-10 h-10 aspect-square rounded-xl bg-white/5 text-slate-400 hover:bg-white/10 hover:text-on-surface transition-colors flex items-center justify-center">
                    <span class="material-symbols-outlined text-sm">chevron_right</span>
                </a>
            }
        </div>
    </div>
</div>

<!-- Add Movie Modal -->
<div id="addMovieModal" class="fixed inset-0 bg-slate-950/80 backdrop-blur-md z-50 hidden flex items-center justify-center p-4">
    <div class="glass-panel w-full max-w-4xl rounded-3xl border border-white/10 p-0 shadow-2xl bg-slate-900/90 relative overflow-hidden">
        <div class="p-6 border-b border-white/5 flex items-center justify-between">
            <h3 class="font-headline font-bold text-xl text-on-surface flex items-center gap-2">
                <span class="w-3 h-3 rounded-full bg-primary shadow-[0_0_10px_#61b4fe]"></span>
                THÊM PHIM MỚI
            </h3>
            <button onclick="document.getElementById('addMovieModal').classList.add('hidden')" class="text-slate-400 hover:text-white transition-colors">
                <span class="material-symbols-outlined">close</span>
            </button>
        </div>
        
        <form id="addMovieForm" class="p-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Left Side: Basic Info -->
                <div class="space-y-4 text-sm">
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Tên phim</label>
                        <input type="text" name="name" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Mô tả</label>
                        <textarea name="description" rows="4" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all"></textarea>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Thời lượng (phút)</label>
                            <input type="number" name="duration" required min="1" max="600" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Hãng phim</label>
                            <input type="text" name="studio" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Đạo diễn</label>
                            <input type="text" name="director" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Thể loại</label>
                            <select name="genre" class="w-full py-2.5 px-4 rounded-xl bg-slate-800 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all">
                                <option value="0">Action</option>
                                <option value="1">Comedy</option>
                                <option value="2">Drama</option>
                                <option value="3">Horror</option>
                                <option value="4">SciFi</option>
                                <option value="5">Romance</option>
                                <option value="6">Thriller</option>
                                <option value="7">Animation</option>
                                <option value="8">Documentary</option>
                            </select>
                        </div>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Trạng thái</label>
                        <select name="status" class="w-full py-2.5 px-4 rounded-xl bg-slate-800 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all">
                            <option value="0">Sắp chiếu</option>
                            <option value="1">Đang chiếu</option>
                            <option value="2">Ngừng chiếu</option>
                        </select>
                    </div>
                </div>

                <!-- Right Side: Media -->
                <div class="space-y-4">
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Thumbnail URL</label>
                        <input type="url" id="addThumbnailUrl" name="thumbnailUrl" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" placeholder="https://..." />
                    </div>
                    
                    <div class="relative group">
                        <label class="block text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-2 ml-1">Upload Thumbnail</label>
                        <div id="addMovieDropZone" class="relative border-2 border-dashed border-white/10 rounded-2xl p-8 text-center cursor-pointer hover:border-primary/40 hover:bg-primary/5 transition-all group overflow-hidden">
                            <input type="file" id="addMovieFileInput" accept="image/jpeg,image/png,image/gif,image/webp,image/avif" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" />
                            
                            <div id="addMovieUploadPrompt">
                                <div class="w-16 h-16 bg-white/5 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 group-hover:bg-primary/10 transition-all duration-500">
                                    <span class="material-symbols-outlined text-3xl text-slate-500 group-hover:text-primary transition-colors">cloud_upload</span>
                                </div>
                                <p class="text-sm font-semibold text-slate-300">Kéo thả hoặc click để chọn ảnh</p>
                                <p class="text-xs text-slate-500 mt-1">Hỗ trợ JPG, PNG, WebP (Max 5MB)</p>
                            </div>

                            <div id="addMoviePreview" class="hidden relative z-20">
                                <img id="addMoviePreviewImg" class="max-h-48 mx-auto rounded-xl shadow-2xl border border-white/10" />
                                <div class="mt-4 flex items-center justify-center gap-3">
                                    <p id="addMovieFileName" class="text-xs text-slate-400 truncate max-w-[150px]"></p>
                                    <button type="button" onclick="clearAddMoviePreview()" class="p-1.5 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 transition-all">
                                        <span class="material-symbols-outlined text-sm">delete</span>
                                    </button>
                                </div>
                            </div>

                            <!-- Upload Loading Overlay -->
                            <div id="addMovieUploadLoading" class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm hidden items-center justify-center z-30">
                                <div class="flex flex-col items-center gap-3">
                                    <div class="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin"></div>
                                    <p class="text-[10px] font-bold text-primary tracking-widest uppercase">Đang tải lên...</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Official Trailer URL (Optional)</label>
                        <input type="url" name="officialTrailerUrl" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" placeholder="https://youtube.com/..." />
                    </div>
                </div>
            </div>

            <div class="flex items-center justify-end gap-3 pt-6 border-t border-white/5 mt-8">
                <button type="button" onclick="document.getElementById('addMovieModal').classList.add('hidden')" class="py-2.5 px-6 rounded-xl bg-white/5 text-slate-300 hover:bg-white/10 text-xs font-bold transition-all">
                    Đóng
                </button>
                <button type="submit" class="py-2.5 px-8 rounded-xl bg-gradient-to-r from-primary to-primary-container text-on-primary text-xs font-bold shadow-[0_4px_15px_rgba(97,180,254,0.3)] hover:shadow-[0_4px_25px_rgba(97,180,254,0.5)] transition-all">
                    Thêm phim
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Update Movie Modal -->
<div id="updateMovieModal" class="fixed inset-0 bg-slate-950/80 backdrop-blur-md z-50 hidden flex items-center justify-center p-4">
    <div class="glass-panel w-full max-w-4xl rounded-3xl border border-white/10 p-0 shadow-2xl bg-slate-900/90 relative overflow-hidden">
        <div class="p-6 border-b border-white/5 flex items-center justify-between">
            <h3 class="font-headline font-bold text-xl text-on-surface flex items-center gap-2">
                <span class="w-3 h-3 rounded-full bg-secondary shadow-[0_0_10px_#00f4fe]"></span>
                CẬP NHẬT PHIM
            </h3>
            <button onclick="document.getElementById('updateMovieModal').classList.add('hidden')" class="text-slate-400 hover:text-white transition-colors">
                <span class="material-symbols-outlined">close</span>
            </button>
        </div>
        
        <form id="updateMovieForm">
            <input type="hidden" name="id" id="updMovieId" />
            <div class="p-8 grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Left Side: Basic Info -->
                <div class="space-y-4 text-sm">
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Tên phim</label>
                        <input type="text" id="updName" name="name" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Mô tả</label>
                        <textarea id="updDescription" name="description" rows="4" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all"></textarea>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Thời lượng (phút)</label>
                            <input type="number" id="updDuration" name="duration" required min="1" max="600" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Hãng phim</label>
                            <input type="text" id="updStudio" name="studio" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Đạo diễn</label>
                            <input type="text" id="updDirector" name="director" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 mb-1">Thể loại</label>
                            <select id="updGenre" name="genre" class="w-full py-2.5 px-4 rounded-xl bg-slate-800 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all">
                                <option value="0">Action</option>
                                <option value="1">Comedy</option>
                                <option value="2">Drama</option>
                                <option value="3">Horror</option>
                                <option value="4">SciFi</option>
                                <option value="5">Romance</option>
                                <option value="6">Thriller</option>
                                <option value="7">Animation</option>
                                <option value="8">Documentary</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Right Side: Media -->
                <div class="space-y-4">
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Thumbnail URL</label>
                        <input type="url" id="updThumbnailUrl" name="thumbnailUrl" required class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                    </div>
                    
                    <div class="relative group">
                        <label class="block text-[10px] font-bold uppercase tracking-widest text-slate-500 mb-2 ml-1">Update Thumbnail</label>
                        <div id="updMovieDropZone" class="relative border-2 border-dashed border-white/10 rounded-2xl p-8 text-center cursor-pointer hover:border-secondary/40 hover:bg-secondary/5 transition-all group overflow-hidden">
                            <input type="file" id="updMovieFileInput" accept="image/jpeg,image/png,image/gif,image/webp,image/avif" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10" />
                            
                            <div id="updMovieUploadPrompt">
                                <div class="w-16 h-16 bg-white/5 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 group-hover:bg-secondary/10 transition-all duration-500">
                                    <span class="material-symbols-outlined text-3xl text-slate-500 group-hover:text-secondary transition-colors">cloud_upload</span>
                                </div>
                                <p class="text-sm font-semibold text-slate-300">Kéo thả hoặc click để thay đổi</p>
                                <p class="text-xs text-slate-500 mt-1">Hỗ trợ JPG, PNG, WebP (Max 5MB)</p>
                            </div>

                            <div id="updMoviePreview" class="hidden relative z-20">
                                <img id="updMoviePreviewImg" class="max-h-48 mx-auto rounded-xl shadow-2xl border border-white/10" />
                                <div class="mt-4 flex items-center justify-center gap-3">
                                    <p id="updMovieFileName" class="text-xs text-slate-400 truncate max-w-[150px]"></p>
                                    <button type="button" onclick="clearUpdMoviePreview()" class="p-1.5 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 transition-all">
                                        <span class="material-symbols-outlined text-sm">delete</span>
                                    </button>
                                </div>
                            </div>

                            <!-- Upload Loading Overlay -->
                            <div id="updMovieUploadLoading" class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm hidden items-center justify-center z-30">
                                <div class="flex flex-col items-center gap-3">
                                    <div class="w-8 h-8 border-2 border-secondary border-t-transparent rounded-full animate-spin"></div>
                                    <p class="text-[10px] font-bold text-secondary tracking-widest uppercase">Đang tải lên...</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-semibold text-slate-400 mb-1">Official Trailer URL (Optional)</label>
                        <input type="url" id="updOfficialTrailerUrl" name="officialTrailerUrl" class="w-full py-2.5 px-4 rounded-xl bg-white/5 border border-white/10 text-slate-200 focus:border-primary/50 focus:ring-primary focus:outline-none transition-all" />
                    </div>
                </div>
            </div>

            <div class="p-6 bg-white/5 flex items-center justify-between">
                <button type="button" onclick="deleteMovie()" class="py-2.5 px-4 rounded-xl bg-red-500/10 text-red-400 hover:bg-red-500/20 text-xs font-bold transition-all flex items-center gap-2">
                    <span class="material-symbols-outlined text-sm">delete</span>
                    Xóa phim
                </button>
                <div class="flex items-center gap-3">
                    <button type="button" onclick="document.getElementById('updateMovieModal').classList.add('hidden')" class="py-2.5 px-6 rounded-xl bg-white/5 text-slate-300 hover:bg-white/10 text-xs font-bold transition-all">
                        Đóng
                    </button>
                    <button type="submit" class="py-2.5 px-8 rounded-xl bg-gradient-to-r from-secondary to-secondary text-on-secondary text-xs font-bold shadow-[0_4px_15px_rgba(0,244,254,0.3)] hover:shadow-[0_4px_25px_rgba(0,244,254,0.5)] transition-all">
                        Lưu thay đổi
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        // =============================================
        // Search Input Logic
        // =============================================
        const searchInput = document.getElementById('searchInput');
        const searchIcon = document.getElementById('searchIcon');
        const searchBtn = document.getElementById('searchBtn');

        function updateSearchIcon() {
            if (searchInput.value.trim() !== '') {
                searchIcon.textContent = 'close';
                searchIcon.classList.add('text-red-500');
                searchIcon.classList.remove('text-sm');
                searchIcon.classList.add('text-base', 'font-bold');
            } else {
                searchIcon.textContent = 'search';
                searchIcon.classList.remove('text-red-500', 'font-bold');
                searchIcon.classList.add('text-sm');
                searchIcon.classList.remove('text-base');
            }
        }

        if (searchInput) {
            searchInput.addEventListener('input', updateSearchIcon);
            updateSearchIcon();
        }

        if (searchBtn) {
            searchBtn.addEventListener('click', function(e) {
                // Chỉ xử lý clear khi icon là 'close' VÀ người dùng thực sự click chuột (e.detail > 0)
                // Nếu nhấn Enter (e.detail === 0), cho phép form submit bình thường
                if (searchIcon.textContent === 'close' && e.detail > 0) {
                    e.preventDefault();
                    searchInput.value = '';
                    
                    const url = new URL(window.location.href);
                    if (url.searchParams.has('searchTerm')) {
                        url.searchParams.delete('searchTerm');
                        url.searchParams.delete('pageNumber');
                        window.location.href = url.toString();
                    } else {
                        updateSearchIcon();
                        searchInput.focus();
                    }
                }
            });
        }

        // =============================================
        // File upload preview helpers
        // =============================================

        let addMovieFile = null;
        let updMovieFile = null;

        function setupFilePreview(inputId, previewContainerId, previewImgId, fileNameId, promptId, loadingId, urlInputId, fileSetter) {
            const input = document.getElementById(inputId);
            input.addEventListener('change', async function () {
                const file = this.files[0];
                if (!file) return;

                // Show loading
                document.getElementById(loadingId).classList.remove('hidden');
                document.getElementById(loadingId).classList.add('flex');

                try {
                    const formData = new FormData();
                    formData.append('file', file);
                    const resp = await fetch('/Movie/UploadThumbnail', { method: 'POST', body: formData });
                    const result = await resp.json();

                    if (result.success) {
                        // Auto-fill URL input
                        document.getElementById(urlInputId).value = result.url;
                        
                        // Update Preview
                        document.getElementById(previewImgId).src = result.url;
                        document.getElementById(fileNameId).textContent = file.name + ' (' + (file.size / 1024).toFixed(1) + ' KB)';
                        document.getElementById(previewContainerId).classList.remove('hidden');
                        document.getElementById(promptId).classList.add('hidden');
                        
                        fileSetter(file);
                        window.showToast('Tải lên ảnh thành công!', 'success');
                    } else {
                        window.showToast(result.message, 'error');
                        input.value = '';
                    }
                } catch (err) {
                    console.error(err);
                    window.showToast('Lỗi khi tải lên ảnh.', 'error');
                    input.value = '';
                } finally {
                    document.getElementById(loadingId).classList.add('hidden');
                    document.getElementById(loadingId).classList.remove('flex');
                }
            });
        }

        setupFilePreview('addMovieFileInput', 'addMoviePreview', 'addMoviePreviewImg', 'addMovieFileName', 'addMovieUploadPrompt', 'addMovieUploadLoading', 'addThumbnailUrl', f => addMovieFile = f);
        setupFilePreview('updMovieFileInput', 'updMoviePreview', 'updMoviePreviewImg', 'updMovieFileName', 'updMovieUploadPrompt', 'updMovieUploadLoading', 'updThumbnailUrl', f => updMovieFile = f);

        function clearAddMoviePreview() {
            addMovieFile = null;
            document.getElementById('addMovieFileInput').value = '';
            document.getElementById('addThumbnailUrl').value = '';
            document.getElementById('addMoviePreview').classList.add('hidden');
            document.getElementById('addMovieUploadPrompt').classList.remove('hidden');
        }

        function clearUpdMoviePreview() {
            updMovieFile = null;
            document.getElementById('updMovieFileInput').value = '';
            document.getElementById('updThumbnailUrl').value = '';
            document.getElementById('updMoviePreview').classList.add('hidden');
            document.getElementById('updMovieUploadPrompt').classList.remove('hidden');
        }

        // =============================================
        // Modal functions
        // =============================================

        function openAddMovieModal() {
            document.getElementById('addMovieForm').reset();
            clearAddMoviePreview();
            document.getElementById('addMovieModal').classList.remove('hidden');
        }

        function openUpdateMovieModal(btn) {
            document.getElementById('updateMovieForm').reset();
            clearUpdMoviePreview();
            
            document.getElementById('updMovieId').value = btn.getAttribute('data-id');
            document.getElementById('updName').value = btn.getAttribute('data-name');
            document.getElementById('updDescription').value = btn.getAttribute('data-desc');
            document.getElementById('updDuration').value = btn.getAttribute('data-duration');
            document.getElementById('updStudio').value = btn.getAttribute('data-studio');
            document.getElementById('updDirector').value = btn.getAttribute('data-director');
            document.getElementById('updThumbnailUrl').value = btn.getAttribute('data-thumb');
            document.getElementById('updOfficialTrailerUrl').value = btn.getAttribute('data-trailer') || '';
            document.getElementById('updGenre').value = btn.getAttribute('data-genre');

            document.getElementById('updateMovieModal').classList.remove('hidden');
        }

        // =============================================
        // Delete
        // =============================================

        async function deleteMovie() {
            const id = document.getElementById('updMovieId').value;
            const name = document.getElementById('updName').value;
            
            if (!confirm(`Bạn có chắc chắn muốn xóa phim "${name}" không?`)) return;

            if (window.showLoading) window.showLoading();
            try {
                const response = await fetch('/Movie/Delete?id=' + id, {
                    method: 'POST'
                });
                const result = await response.json();

                if (result.success) {
                    window.showToast(result.message, 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    window.showToast(result.message, 'error');
                }
            } catch (err) {
                console.error(err);
                window.showToast('Có lỗi xảy ra khi xóa phim.', 'error');
            } finally {
                if (window.hideLoading) window.hideLoading();
            }
        }

        // =============================================
        // Form submissions
        // =============================================

        document.getElementById('addMovieForm').addEventListener('submit', async function (e) {
            e.preventDefault();
            const formData = new FormData(this);
            const payload = Object.fromEntries(formData.entries());
            
            payload.duration = parseInt(payload.duration);
            payload.genre = parseInt(payload.genre);
            payload.status = parseInt(payload.status);

            if (window.showLoading) window.showLoading();
            try {
                const response = await fetch('/Movie/Create', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();

                if (result.success) {
                    window.showToast(result.message, 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    window.showToast(result.message, 'error');
                }
            } catch (err) {
                console.error(err);
                window.showToast('Có lỗi xảy ra khi xử lý yêu cầu.', 'error');
            } finally {
                if (window.hideLoading) window.hideLoading();
            }
        });

        document.getElementById('updateMovieForm').addEventListener('submit', async function (e) {
            e.preventDefault();

            const formData = new FormData(this);
            const payload = Object.fromEntries(formData.entries());
            
            payload.duration = parseInt(payload.duration);
            payload.genre = parseInt(payload.genre);

            if (window.showLoading) window.showLoading();
            try {
                const response = await fetch('/Movie/Update', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();

                if (result.success) {
                    window.showToast(result.message, 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    window.showToast(result.message, 'error');
                }
            } catch (err) {
                console.error(err);
                window.showToast('Có lỗi xảy ra khi xử lý yêu cầu.', 'error');
            } finally {
                if (window.hideLoading) window.hideLoading();
            }
        });
    </script>
}
